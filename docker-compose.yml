version: "3.9"

services:
  kup-piksel:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - pixel-data:/app/data
      - ./backend/config.json:/app/config.json:ro

volumes:
  pixel-data:
